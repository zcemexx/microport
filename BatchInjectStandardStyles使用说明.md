# BatchInjectStandardStyles 使用说明

`BatchInjectStandardStyles.bas` 是一个 Microsoft Word VBA 模块。它会递归扫描所选文件夹中的 Word 文档，为每份可处理文档建立或更新统一样式、设置页边距，并将格式化后的副本输出到单独目录。

## 使用前准备

- 建议先备份待处理文件夹。宏不会修改源文件，但重新运行时会覆盖同名的旧输出文件。
- 请在 Windows 版 Microsoft Word 中运行，并确保 Word 可以正常打开待处理文档。
- 待处理文档应未被密码保护、未被其他用户或程序锁定，且当前用户具有读取权限。
- 宏支持 `.doc`、`.docx`、`.docm` 和 `.docb` 文件；以 `~$` 开头的 Word 临时文件会自动忽略。

## 导入并运行批处理宏

1. 打开 Microsoft Word，按 `Alt + F11` 打开 VBA 编辑器。
2. 在左侧项目窗口中选中要存放宏的文档或 `Normal` 模板。
3. 选择“文件”→“导入文件”，选择 `BatchInjectStandardStyles.bas`。
4. 如需长期保存宏，请将宿主文档保存为启用宏的格式（例如 `.docm`）或保存在 `Normal` 模板中。
5. 返回 Word，按 `Alt + F8`，选择 `BatchInjectStandardStyles`，点击“运行”。
6. 在弹出的文件夹选择窗口中，选择包含待处理 Word 文件的根文件夹。
7. 等待完成提示。批量处理期间 Word 会暂时关闭屏幕刷新和警告提示。

宏会递归处理所选根文件夹及其子文件夹。它会忽略根目录下的 `formatted_output` 文件夹，因而不会将已输出的文件再次处理。

## 输出位置与文件安全

输出文件保存在所选文件夹下的 `formatted_output` 目录中，并保留源文件的子目录结构。例如：

```text
选择的文件夹
├── 项目A\报告.docx
└── formatted_output
    └── 项目A\报告.docx
```

- 源文件不会保存或覆盖；宏始终生成格式化副本。
- 输出文件使用与源文件相同的保存格式和扩展名。
- 若输出目录中已存在同一路径、同名文件，旧的输出副本会被新结果替换。
- 处理 `.docm` 时，文档中的 `AutoOpen` 等自动宏会被临时禁用，以避免在批处理过程中执行。

## 宏会设置的格式

| 对象 | 字体与字号 | 段落设置 |
| --- | --- | --- |
| 正文（“正文”样式） | 中文宋体；西文 Times New Roman；小四（12 磅） | 1.5 倍行距，段前 0 行、段后 1 行 |
| 标题 1、标题 2 | 中文宋体；西文 Times New Roman；小四（12 磅）；加粗 | 1.5 倍行距，段前 1 行、段后 1 行 |
| 标题 3 至标题 9 | 中文宋体；西文 Times New Roman；小四（12 磅）；不加粗 | 1.5 倍行距，段前 1 行、段后 1 行 |
| `Cover Title` | 中文宋体；西文 Times New Roman；一号（26 磅）；加粗 | 居中，单倍行距，段前段后均为 0 |
| `Table Title` | 中文宋体；西文 Times New Roman；五号（10.5 磅）；加粗 | 居中，单倍行距，段前段后均为 0 |
| `Table Content` | 中文宋体；西文 Times New Roman；五号（10.5 磅）；不加粗 | 左对齐，单倍行距，段前段后均为 0 |

其中 `Cover Title`、`Table Title` 和 `Table Content` 为宏创建或更新的英文命名段落样式；若同名样式已存在，会按上表重新设置。正文及标题样式也会被更新，且文档中已具有 1–9 级大纲级别的段落会同步获得标题的段前、段后与行距设置。

### 页边距

宏会逐节依据页面方向设置页边距，混合横竖版文档也可处理：

| 页面方向 | 上/下 | 左/右 |
| --- | --- | --- |
| 纵向 | 2.5 cm | 2 cm |
| 横向 | 1.2 cm | 1.2 cm |

### 写入的快捷键

每个成功处理的文档会写入下列样式快捷键：

| 快捷键 | 样式 |
| --- | --- |
| `Alt + 1` | 标题 1 |
| `Alt + 2` | 标题 2 |
| `Alt + 3` | 标题 3 |
| `Alt + Q` | `Cover Title` |
| `Alt + W` | `Table Title` |
| `Alt + E` | `Table Content` |

这些组合键若在文档中已有绑定，Word 的实际行为可能受现有键位设置影响；如有冲突，请在 Word 的“自定义键盘”中调整。

## 仅设置当前文档页边距

模块还提供 `AutoSetMarginsByOrientation` 宏。打开需要调整的 Word 文档后，按 `Alt + F8` 运行此宏，即可只按页面方向重设当前文档每一节的页边距，不会批量处理文件，也不会更改样式。

## 结果与排错

完成时会显示成功、跳过和失败数量，以及输出目录。被跳过或失败的文件及原因会写入 VBA 编辑器的“立即窗口”；在 VBA 编辑器中按 `Ctrl + G` 即可打开该窗口查看。

常见跳过原因：

- 文件是承载此宏的文档本身；
- 文档以只读方式打开；
- 文档受到保护。

常见失败原因包括文件损坏、格式不受 Word 支持、路径或文件名异常、输出目录无写入权限，以及文档正在被占用。请根据立即窗口中的错误信息处理后重新运行；已成功生成的输出文件可以保留。
